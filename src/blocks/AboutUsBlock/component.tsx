import React from 'react'
import type { ProjecstTitle } from '@/payload-types'

export const AboutUsBlock: React.FC<ProjecstTitle> = ({ title }) => {
  return (
    <div className="text-center">
        About Us
    </div>
  )
}
